loki:
  serviceName: loki
  servicePort: 3100

config:
  labelMap:
    kubernetes:
      namespace_name: namespace
      pod_name: pod
      container_name: container
    stream: stream

resources:
  requests:
    cpu: 10m
    memory: 64Mi
